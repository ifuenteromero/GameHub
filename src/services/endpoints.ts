const baseUrl = 'https://api.rawg.io/api';
const games = '/games';
const genres = '/genres';
const platforms = '/platforms/lists/parents';

export default {
    baseUrl,
    games,
    genres,
    platforms,
};
